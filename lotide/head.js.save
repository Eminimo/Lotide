// assertEqual.js

const assertEqual = function(actual, expected) {
  if (actual === expected) {
    console.log(`✅✅✅ Assertion Passed: ${actual} === ${expected}`);
  } else {
    console.log(`🛑🛑🛑 Assertion Failed: ${actual} !== ${expected}`);
  }
};

// head.js

// Copy the assertEqual function from assertEqual.js and paste it here

const head = function(arr) {
  // Implement the head function logic here
};

// Test cases using assertEqual
assertEqual(head([5, 6, 7]), 5);
assertEqual(head(["Hello", "Lighthouse", "Labs"]), "Hello");
// Add more test cases as mentioned in the instructions

const head = function(arr) {
  // Implement the head function logic here
};

assertEqual(head([5, 6, 7]), 5);
assertEqual(head(["Hello", "Lighthouse", "Labs"]), "Hello");
// Add more test cases as mentioned in the instructi
